import { DocumentOverview } from './document-overview.model';

describe('DocumentOverview', () => {
  it('should create an instance', () => {
    expect(new DocumentOverview()).toBeTruthy();
  });
});
